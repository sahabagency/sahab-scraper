(() => {
  const form = document.querySelector('#scan-form');
  const status = document.querySelector('#scan-status');
  const result = document.querySelector('#scan-result');
  const languageSelect = document.querySelector('#language');
  if (!form || !status || !result) return;

  const esc = (value = '') => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money = n => Number(n || 0).toLocaleString('en-US');
  const normalizeUrl = value => /^https?:\/\//i.test(String(value || '').trim()) ? String(value).trim() : `https://${String(value || '').trim()}`;
  const nameFromUrl = value => { try { return new URL(normalizeUrl(value)).hostname.replace(/^www\./,'').split('.')[0].replace(/[-_]+/g,' '); } catch { return value; } };
  const isEnglish = () => languageSelect?.value === 'en';
  const hasArabic = value => /[\u0600-\u06FF]/.test(String(value || ''));
  const safeEn = (value, fallback = 'Not verified from public data') => hasArabic(value) ? fallback : (value || fallback);
  const serviceEn = value => ({'بحث ونمو الطلب':'Search & category demand growth','نمو الطلب والتصنيفات':'Search & category demand growth','تحسين عرض المنتجات والتحويل':'Product merchandising & CRO','جودة المحتوى والثقة':'Content & trust quality','تفعيل الاحتفاظ والولاء':'Retention & loyalty activation'}[value] || safeEn(value, 'Conversion improvement')); 
  const ri = n => isEnglish() ? `SAR ${money(n)}` : `${money(n)} ريال`;
  const platformName = bi => typeof bi.platform === 'string' ? bi.platform : (bi.platform?.name || '');
  const serviceAr = value => ({'Search & category demand growth':'نمو الطلب والبحث والتصنيفات','Product merchandising & CRO':'تحسين عرض المنتجات والتحويل','Service & offer merchandising / booking CRO':'تحسين عرض الخدمات والباقات والحجز','Retention & loyalty activation':'تفعيل الاحتفاظ والولاء','B2B / project pipeline expansion':'توسيع مسار مشاريع B2B','B2B / project lead capture':'التقاط طلبات مشاريع B2B'}[value] || value);
  const serviceLabel = value => isEnglish() ? serviceEn(value) : serviceAr(value);
  const arIndustry = value => ({'aesthetic clinic':'عيادات تجميل','general ecommerce':'تجارة إلكترونية','water coolers, water dispensers & storage solutions':'برادات ومبردات وخزانات مياه'}[value] || value);
  const arModel = value => ({'Direct ecommerce':'تجارة إلكترونية مباشرة','Direct ecommerce (primary) + B2B/project sales (secondary)':'تجارة إلكترونية مباشرة (أساسي) + مبيعات مشاريع B2B (ثانوي)','ecommerce + B2B/project sales':'تجارة إلكترونية + مبيعات مشاريع B2B','B2B / project sales':'مبيعات المشاريع والشركات'}[value] || value);
  const arText = value => ({'Cart / checkout':'السلة / إتمام الطلب','B2B / projects':'المشاريع والشركات','Quote request':'طلب عرض سعر','WhatsApp':'واتساب','Reviews':'التقييمات','Loyalty':'الولاء','Installments':'التقسيط','Delivery':'التوصيل','Consumers / homes':'المستهلكون / المنازل','Projects / companies':'المشاريع / الشركات','Mosques / charity':'المساجد / الجهات الخيرية','Farms / outdoor':'المزارع / الاستخدام الخارجي','Saudi-made positioning':'تموضع سعودي الصنع','Free delivery':'توصيل مجاني','Warranty':'الضمان','Loyalty program':'برنامج الولاء','Made for Saudi climate':'مصمم لمناخ السعودية'}[value] || value);

  async function scan(payload) {
    const response = await fetch('/api/leads/audit', { method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(payload) });
    const data = await response.json();
    if (!response.ok) throw new Error(data.error || 'Scan failed');
    return data;
  }

  function chip(text) { return `<span style="display:inline-block;padding:5px 9px;border:1px solid #5a5136;border-radius:999px;margin:3px 4px 3px 0;font-size:12px;color:#d9c880">${esc(text)}</span>`; }
  function sourceLabel(value='') {
    const map = {
      observed_site_prices:'أسعار المنتجات المرصودة فعليًا من المتجر', observed_public_product_prices:'أسعار المنتجات المرصودة فعليًا من المتجر',
      bounded_review_activity_proxy:'نشاط عام محدود من التقييمات', public_purchase_counter_activity_envelope:'مؤشرات شراء عامة من صفحات المنتجات',
      business_model_prior:'سيناريو محافظ حسب نموذج النشاط', category_demand_envelope:'تقدير محافظ حسب نوع النشاط', public_activity_demand_envelope:'نشاط عام + بصمة المتجر',
      campaign_anchor:'قيمة مدخلة في الحملة', campaign_anchor_plus_public_activity:'قيمة حملة + نشاط عام', secondary_b2b_project_scenario:'سيناريو مستقل لمسار المشاريع B2B'
    };
    return isEnglish() ? ({observed_site_prices:'Observed site prices',observed_public_product_prices:'Observed public product prices',bounded_review_activity_proxy:'Bounded public review activity',public_purchase_counter_activity_envelope:'Public purchase activity indicators',business_model_prior:'Conservative business-model scenario',category_demand_envelope:'Conservative category-demand scenario',public_activity_demand_envelope:'Public activity plus site footprint',campaign_anchor:'Campaign input',campaign_anchor_plus_public_activity:'Campaign input plus public activity',secondary_b2b_project_scenario:'Separate B2B project scenario'}[value] || safeEn(value)) : (map[value] || value || 'استدلال من بيانات المشروع العامة');
  }
  function englishPlan(service='') { const map={'Search & category demand growth':['The site has real categories, but search coverage across purchase intents is not fully developed.','Expand category and landing pages around search intent, product, and use case.','Build a keyword map, publish intent-specific pages, add internal links, then test impressions and clicks.','Track qualified keywords, organic sessions, and category-page clicks.'],'Product merchandising & CRO':['The catalog and buying path exist, but product ordering, comparison, and offers need structured testing.','Rank products by demand and margin, add comparisons and bundles, and improve the CTA.','Use internal search data, click maps, and weekly category/product experiments.','Track product CTR, add-to-cart, average order value, and conversion rate.'],'Retention & loyalty activation':['The loyalty program is visible, but activation and repeat-purchase behavior need structured testing.','Improve loyalty onboarding, triggers, and repeat-purchase offers.','Segment members, test lifecycle messages, and compare repeat purchase cohorts.','Track activation, repeat purchase rate, and revenue per member.']}; const x=map[service]||['A specific improvement area was identified from public site evidence.','Turn the observation into a page-specific conversion experiment.','Set a baseline, implement one change, and compare before and after.','Track the conversion metric tied directly to the problem.']; return {problem:x[0],solution:x[1],method:x[2],measurement:x[3]}; }

  function tri(label, tracker) {
    const state = tracker?.state || 'unknown';
    const yes = state === 'verified', unknown = state === 'unknown';
    const color = yes ? '#c7e4b5' : unknown ? '#e0c878' : '#d7a0a0';
    const symbol = yes ? '●' : unknown ? '◐' : '○';
    const suffix = isEnglish() ? (yes ? 'Verified' : unknown ? 'Not publicly observable' : 'Not observed') : (yes ? 'مؤكد من كود الموقع' : unknown ? 'لم يظهر علنًا' : 'غير مرصود');
    const evidence = (tracker?.evidence || []).slice(0,2).join(' · ');
    const reason = yes ? evidence : (isEnglish() ? 'No public marker in sampled pages; not proof of absence.' : 'لم يظهر مؤشر عام في الصفحات المفحوصة؛ لا يعني أنه غير موجود.');
    return `<span style="display:inline-block;margin:4px 10px 4px 0;font-size:12px;color:${color}" title="${esc(reason)}">${symbol} ${esc(label)} · ${suffix}${evidence ? `<small style="display:block;color:#a89d78;margin-left:15px;max-width:290px;white-space:normal">${esc(evidence)}</small>` : ''}</span>`;
  }

  function render(data) {
    const lead = data.lead || {}, audit = data.audit || {}, bi = audit.businessIntelligence || {}, profile = audit.commercialProfile || {};
    const q = data.qualification || audit.qualification || {}, opportunity = audit.opportunity || {}, monthly = opportunity.monthlyRange || {low:0,high:0};
    const english = languageSelect?.value === 'en'; const labels = english ? { problem:'Problem', evidence:'Evidence', solution:'Solution', method:'Method', measurement:'Measurement', verified:'Verified finding', modeled:'Modeled growth opportunity', month:'SAR / month modeled' } : { problem:'المشكلة المحددة', evidence:'الدليل', solution:'الحل', method:'طريقة التنفيذ', measurement:'القياس', verified:'فجوة مثبتة', modeled:'فرصة نمو نمذجية', month:'ريال / شهر تقديري' };
    const breakdown = audit.opportunityBreakdown || [], confidence = opportunity.confidence || bi.confidence || 0;
    const priceStats = bi.priceStats || profile.observedPriceStats || {}, categories = (bi.categories || []).slice(0,10) .map(x => isEnglish() ? safeEn(x, 'Category not verified') : arText(x)), products = (bi.products || []).slice(0,10) .map(x => isEnglish() ? safeEn(x, 'Product not verified') : arText(x));
    const segments = (bi.targetSegments || bi.audiences || []).slice(0,8) .map(x => isEnglish() ? safeEn(x, 'Audience not verified') : arText(x)), values = (bi.valuePropositions || []).slice(0,8) .map(x => isEnglish() ? safeEn(x, 'Value proposition not verified') : arText(x)), funnel = bi.funnelSignals || {};
    const pageInv = bi.pageInventory || {}, catalog = bi.catalog || {}, pName = platformName(bi), baseline = opportunity.businessBaseline || {};
    const primary = opportunity.primaryRevenueMotion || bi.primaryRevenueMotion || (bi.commerce?.ecommerce ? 'Direct ecommerce' : '');
    const secondary = opportunity.secondaryRevenueMotions || bi.secondaryRevenueMotions || [];
    const unitLabel = isEnglish() ? (profile.volumeUnit === 'orders' ? 'orders' : 'leads') : (profile.volumeUnit === 'orders' ? 'طلبات شراء' : 'فرص/Leads');
    const b2bScenario = profile.secondaryB2BScenario || null;

    const rows = breakdown.slice(0,6).map(item => {
      const hi = Math.round(Number(item.annualRange?.high || 0)/12), lo = Math.round(Number(item.annualRange?.low || 0)/12);
      const evidenceTag = item.evidenceClass === 'verified_gap' ? labels.verified : labels.modeled;
      return `<div class="leak-row finding"><div><h3>${esc(serviceLabel(item.service))}</h3><p><strong>${labels.problem}:</strong> ${esc(english ? safeEn(item.problemEn, englishPlan(serviceEn(item.service)).problem) : (item.problem || ''))}</p><p><strong>${labels.evidence}:</strong> ${esc(english ? safeEn(item.evidenceEn, englishPlan(serviceEn(item.service)).problem) : (item.issues || []).join(' · '))}</p><p><strong>${labels.solution}:</strong> ${esc(english ? safeEn(item.solutionEn, englishPlan(serviceEn(item.service)).solution) : (item.solution || ''))}</p><p><strong>${labels.method}:</strong> ${esc(english ? safeEn(item.methodEn, englishPlan(serviceEn(item.service)).method) : (item.method || ''))}</p><p><strong>${labels.measurement}:</strong> ${esc(english ? safeEn(item.measurementEn, englishPlan(serviceEn(item.service)).measurement) : (item.measurement || ''))}</p><p style="font-size:11px;color:#c9b76e">${esc(evidenceTag)} · ${money(item.confidence || 0)}%</p></div><div class="leak-value">${money(hi)}<small>${money(lo)}–${money(hi)} ${esc(labels.month)}</small></div></div>`;
    }).join('');

    const known = (isEnglish() ? [
      pName ? `Platform: ${safeEn(/salla/i.test(pName)?'Salla':pName)}` : null, bi.industry ? `Industry: ${safeEn(bi.industry)}` : null, bi.businessModel ? `Model: ${safeEn(bi.businessModel)}` : null, primary ? `Primary revenue: ${safeEn(primary)}` : null, secondary.length ? `Secondary motion: ${secondary.map(x=>safeEn(x)).join(' + ')}` : null, (catalog.productCount || pageInv.productLinks) ? `Products: ${catalog.productCount || pageInv.productLinks}` : null, (catalog.categoryCount || pageInv.categoryLinks) ? `Categories: ${catalog.categoryCount || pageInv.categoryLinks}` : null, priceStats.sampleCount ? `Observed prices: ${priceStats.sampleCount} samples` : null, priceStats.median ? `Median price: ${ri(priceStats.median)}` : null, priceStats.min && priceStats.max ? `Price range: ${ri(priceStats.min)}–${ri(priceStats.max)}` : null
    ] : [pName ? `المنصة: ${/salla/i.test(pName)?'سلة':pName}` : null, bi.industry ? `النشاط: ${arIndustry(bi.industry)}` : null, bi.businessModel ? `النموذج: ${arModel(bi.businessModel)}` : null, primary ? `الإيراد الأساسي: ${arModel(primary)}` : null, secondary.length ? `مسار ثانوي: ${secondary.map(arModel).join(' + ')}` : null, (catalog.productCount || pageInv.productLinks) ? `المنتجات: ${catalog.productCount || pageInv.productLinks}` : null, (catalog.categoryCount || pageInv.categoryLinks) ? `التصنيفات: ${catalog.categoryCount || pageInv.categoryLinks}` : null, priceStats.sampleCount ? `أسعار مرصودة: ${priceStats.sampleCount} عينة` : null, priceStats.median ? `وسيط السعر: ${ri(priceStats.median)}` : null, priceStats.min && priceStats.max ? `نطاق الأسعار: ${ri(priceStats.min)}–${ri(priceStats.max)}` : null]).filter(Boolean).map(chip).join('');

    const funnelRows = [
      [isEnglish()?'Cart / checkout':'السلة / إتمام الطلب', funnel.cartDetected || funnel.checkoutDetected], [isEnglish()?'B2B / projects':'المشاريع والشركات', funnel.b2bPageDetected || funnel.b2bSecondaryDetected || funnel.b2bDetected],
      [isEnglish()?'Quote request':'طلب عرض سعر', funnel.quoteRequestDetected || funnel.b2bConversionDetected], [isEnglish()?'WhatsApp':'واتساب', funnel.whatsappDetected], [isEnglish()?'Reviews':'التقييمات', funnel.reviewsDetected], [isEnglish()?'Loyalty':'الولاء', funnel.loyaltyDetected], [isEnglish()?'Installments':'التقسيط', funnel.installmentDetected], [isEnglish()?'Delivery':'التوصيل', funnel.shippingDetected || funnel.freeDeliveryDetected]
    ].map(([label,yes]) => `<span style="display:inline-block;margin:4px 8px 4px 0;font-size:12px;color:${yes?'#c7e4b5':'#9d9789'}">${yes?'●':'○'} ${esc(label)}</span>`).join('');

    const trackers = funnel.trackers || {}; const stackRows = [tri('Google Tag Manager', trackers.googleTagManager), tri('Google Analytics', trackers.googleAnalytics), tri('Meta Pixel', trackers.metaPixel), tri('TikTok Pixel', trackers.tiktokPixel), tri('Snapchat Pixel', trackers.snapchatPixel), tri('Google Ads tag', trackers.googleAds)].join('');
    const email = data.emailHtml && (!isEnglish() || !hasArabic(data.emailHtml)) ? `<details class="outreach-preview" open><summary>${isEnglish()?'Exact English email design that will be sent':'تصميم البريد الذي سيتم إرساله'}</summary><p><strong>${esc(data.outreach?.subject || '')}</strong></p><iframe title="Email preview" style="width:100%;height:760px;border:1px solid #3a362d;border-radius:16px;background:#fff" sandbox="" srcdoc="${esc(data.emailHtml)}"></iframe><details><summary>Plain-text fallback</summary><pre>${esc(data.outreach?.body || '')}</pre></details></details>` : '';

    const baselineHtml = baseline.monthlyCommerceScenarioRange ? `<p><strong>${isEnglish()?'Direct-commerce baseline before improvement:':'سيناريو التجارة المباشرة قبل التحسين:'}</strong> ${ri(baseline.monthlyCommerceScenarioRange.low)}–${ri(baseline.monthlyCommerceScenarioRange.high)} ${isEnglish()?' / month · based on observed median price ':' شهريًا · مبني على وسيط سعر مرصود'} ${ri(baseline.medianObservedTicket)} ${isEnglish()?`and a modeled range of ${money(baseline.modeledOrderRange?.low)}–${money(baseline.modeledOrderRange?.high)} orders/month; this is a scenario, not reported revenue.`:`ونطاق ${money(baseline.modeledOrderRange?.low)}–${money(baseline.modeledOrderRange?.high)} طلب شراء شهريًا كنموذج وليس رقم مبيعات فعلي.`}</p>` : '';
    const upliftHtml = opportunity.combinedUpliftRange ? `<p><strong>${isEnglish()?'Combined improvement range used:':'نسبة التحسين المجمعة المستخدمة:'}</strong> ${money(opportunity.combinedUpliftRange.low)}%–${money(opportunity.combinedUpliftRange.high)}% ${isEnglish()?'after preventing double-counting across channels.':'بعد منع double-counting بين القنوات.'}</p>` : '';
    const b2bHtml = b2bScenario ? `<p><strong>${isEnglish()?'B2B project path modeled separately:':'مسار المشاريع B2B محسوب منفصلًا:'}</strong> ${isEnglish()?`Modeled opportunity ${ri(b2bScenario.opportunityValueRange?.low)}–${ri(b2bScenario.opportunityValueRange?.high)} × ${money(b2bScenario.modeledMonthlyOpportunityCount?.low)}–${money(b2bScenario.modeledMonthlyOpportunityCount?.high)} potential opportunities/month · confidence ${money(b2bScenario.confidence)}% · not store AOV or confirmed revenue.`:`قيمة فرصة نموذجية ${ri(b2bScenario.opportunityValueRange?.low)}–${ri(b2bScenario.opportunityValueRange?.high)} × ${money(b2bScenario.modeledMonthlyOpportunityCount?.low)}–${money(b2bScenario.modeledMonthlyOpportunityCount?.high)} فرص محتملة بالشهر · ثقة ${money(b2bScenario.confidence)}% · ليس هذا متوسط طلب المتجر ولا إيرادًا مؤكدًا.`}</p>` : '';

    const emailNotice = isEnglish() && data.emailHtml && hasArabic(data.emailHtml) ? `<details class="outreach-preview" open><summary>English email preview</summary><p>English email preview is withheld because the generated message failed the language check. Sending remains locked until a clean English draft is available.</p></details>` : '';
    result.innerHTML = `<div class="scan-result-card">
      <div class="business-line"><div><h2>${esc(english ? safeEn(bi.brandName || lead.name || nameFromUrl(lead.website || 'Business'), nameFromUrl(lead.website || 'Business')) : (bi.brandName || lead.name || nameFromUrl(lead.website || 'Business')))}</h2><div class="business-meta">${lead.website ? `<a href="${esc(lead.website)}" target="_blank" style="color:#d7bd68">${esc(lead.website)}</a>` : ''}${lead.contactRoute?.channel ? ` · best contact: ${esc(lead.contactRoute.channel)}` : ''}</div><div style="margin-top:8px">${q.tier ? chip(`${q.tier} · ${q.score}/100 · ${q.label || ''}`) : ''}</div></div><div class="confidence">estimate confidence<b>${money(confidence)}%</b></div></div>

      <div class="leak-row" style="display:block"><h3>${isEnglish()?'Business intelligence from the URL':'فهم النشاط من الرابط'}</h3><p>${isEnglish()?'The audit starts from observable business evidence: platform, catalog, prices, primary and secondary revenue paths, customer segments, and marketing stack.':'التحليل يبدأ من المشروع نفسه قبل أي رقم: النشاط الحقيقي، المنصة، الكتالوج، الأسعار، مسار الشراء الأساسي، المسارات الثانوية، الشرائح، والـmarketing stack.'}</p><div>${known}</div><div style="margin-top:6px">${funnelRows}</div>${categories.length?`<p style="margin-top:10px"><strong>${isEnglish()?'Categories:':'التصنيفات:'}</strong> ${esc(categories.join(isEnglish()? ', ':'، '))}</p>`:''}${products.length?`<p><strong>${isEnglish()?'Observed products:':'منتجات مرصودة:'}</strong> ${esc(products.join(isEnglish()? ', ':'، '))}</p>`:''}${segments.length?`<p><strong>${isEnglish()?'Customer segments:':'شرائح العملاء:'}</strong> ${esc(segments.join(isEnglish()? ', ':'، '))}</p>`:''}${values.length?`<p><strong>${isEnglish()?'Value propositions:':'عناصر القوة:'}</strong> ${esc(values.join(isEnglish()? ', ':'، '))}</p>`:''}<div style="margin-top:9px"><strong>${isEnglish()?'Marketing stack:':'منظومة التتبع والتسويق:'}</strong> ${stackRows}</div></div>

      <div class="leak-box"><div class="label">${isEnglish()?'ESTIMATED MONTHLY GROWTH OPPORTUNITY':'فرصة النمو الشهرية التقديرية'}</div><div class="amount">${money(monthly.high)} <span style="font-size:.42em">${isEnglish()?'SAR':'ريال'}</span></div><div class="range">${isEnglish()?`Modeled range ${money(monthly.low)}–${money(monthly.high)} SAR / month · not realized loss`:`${isEnglish()?`Modeled range ${money(monthly.low)}–${money(monthly.high)} SAR / month · not realized loss`:`النطاق التقديري ${money(monthly.low)}–${money(monthly.high)} ريال / شهر · ليس خسارة محققة`}`}</div></div>

      <div class="leak-row" style="display:block"><h3>${isEnglish()?'Commercial model built from this business':'النموذج التجاري المبني على النشاط'}</h3><p>${isEnglish()?`Direct order value ${ri(profile.averageTicketRange?.low)}–${ri(profile.averageTicketRange?.high)} · modeled monthly volume ${money(profile.monthlyLeadRange?.low)}–${money(profile.monthlyLeadRange?.high)} ${esc(unitLabel)} · value source: ${esc(sourceLabel(profile.averageTicketSource || bi.averageTicketSource))} · volume source: ${esc(sourceLabel(profile.monthlyLeadSource || bi.monthlyLeadSource))}`:`قيمة الطلب المباشر ${ri(profile.averageTicketRange?.low)}–${ri(profile.averageTicketRange?.high)} · سيناريو الحجم الشهري ${money(profile.monthlyLeadRange?.low)}–${money(profile.monthlyLeadRange?.high)} ${esc(unitLabel)} · مصدر القيمة: ${esc(sourceLabel(profile.averageTicketSource || bi.averageTicketSource))} · مصدر الحجم: ${esc(sourceLabel(profile.monthlyLeadSource || bi.monthlyLeadSource))}`}</p>${baselineHtml}${b2bHtml}${upliftHtml}<p style="color:#9d9789">${isEnglish()?'Direct commerce is the base when the site sells online. B2B is modeled separately. Tracking pixels and social channels are not treated as gaps unless absence is verified.':'التجارة المباشرة هي الأساس إذا المتجر يبيع أونلاين. B2B لا يرفع متوسط طلب المتجر؛ يُنمذج كمسار مستقل. Pixel أو السوشيال لا يدخلان كفجوات إذا لم نقدر نثبت غيابهما.'}</p></div>
      ${rows || `<div class="leak-row"><div><h3>${isEnglish()?'No financially supported opportunity in the current sample':'لا توجد فرصة مالية مدعومة بما يكفي في العينة الحالية'}</h3><p>${isEnglish()?'Business, product, and price evidence remains visible, but no number is raised without a defensible lever.':'يظل تحليل النشاط والمنتجات والأسعار ظاهرًا، لكن ما نرفع رقمًا بدون lever قابل للدفاع عنه.'}</p></div></div>`}
      ${email || emailNotice}
    </div>`;
  }

  form.addEventListener('submit', async event => {
    event.preventDefault(); event.stopImmediatePropagation();
    const website = normalizeUrl(form.website.value), button = form.querySelector('button'); button.disabled = true; result.innerHTML = '';
    status.textContent = 'أحلل المشروع من الرابط: النشاط، المنصة، المنتجات، الأسعار، مسار الإيراد الأساسي والثانوي، العملاء، والـmarketing stack ثم أبني التقدير…';
    try { const data = await scan({ lead:{ name:nameFromUrl(website), website }, language: languageSelect?.value === 'en' ? 'en' : 'ar', assumptions:{} }); render(data); status.textContent = 'Live scan complete · business inferred directly from URL · public data only · figures are estimates'; }
    catch (error) { status.textContent = `Scan error: ${error.message}`; }
    finally { button.disabled = false; }
  }, true);
})();

